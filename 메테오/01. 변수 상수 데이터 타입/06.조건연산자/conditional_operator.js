// 조건 ? 값1(true) : 값2(false);
let age = 20;
let isAdult = age > 18 ? true : false;
console.log(isAdult);

// 조건1 ? 값1 : 조건2 ? 값2 : 값3
let point = 70;
let grade = point > 90 ? "A" : point >= 80 ? "B" : "C";
console.log(grade);
