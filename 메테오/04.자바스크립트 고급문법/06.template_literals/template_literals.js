const hello = (name) => {
  console.log(" Hello " + name + ". Wellcome!!!");
};

hello("메테오");

// Template Literals 사용
const templateLiterals = (name) => {
  console.log(`안녕하세요 ${name}쿤`);
};

templateLiterals("메테오");
